'use strict';

class AnimalShelter {
  constructor() {
    this.dogQ = [];
    this.catQ = [];
  }

  enqueue(animal) {
    if (animal === 'cat') {
      animal.push(catQ);
    } else if (animal === 'dog') {
      animal.push(dogQ);
    }
  }

  dequeue(pref) {
    
  }

}